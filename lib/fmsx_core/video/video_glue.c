/* video_glue.c - translation unit for video/AVideo.i.
 *
 * AVideo.i (vendored from ESPlay-fMSX, itself derived from fMSX-go) is
 * written as a ".i" include file rather than a standalone .c file, so it
 * needs a real .c file to pull it into the build. This is that file.
 */
#include "AVideo.i"
